import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  mainWrapper: {
    flex: 1,
    padding: 20,
  },
  backgroundImage: {
    height: "100%",
  },
});

export default styles;
